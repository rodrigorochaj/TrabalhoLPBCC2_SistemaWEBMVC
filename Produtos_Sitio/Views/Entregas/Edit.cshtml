@model Produtos_Sitio.Models.Entrega

@{
    ViewData["Title"] = "Entrega - Editar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="btn-topo">
    <a asp-action="Index" class="btn btn-primary">Voltar</a>
</div>
<h1>Editar Entrega</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id" />
            <div class="form-group">
                <label asp-for="data_entrega" class="control-label"></label>
                <input asp-for="data_entrega" class="form-control" />
                <span asp-validation-for="data_entrega" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="entregue" class="control-label"></label>
                <select asp-for="entregue" class="form-control"></select>
                <span asp-validation-for="entregue" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Atualizar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
